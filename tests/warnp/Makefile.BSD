# Program name.
PROG	=	test_warnp

# Don't install it.
NOINST	=	1

# Useful relative directories
LIBCPERCIVA_DIR	=	../..

# Main test code
SRCS	=	main.c

# Utility functions
.PATH.c :	${LIBCPERCIVA_DIR}/util
SRCS	+=	warnp.c
IDIRS	+=	-I${LIBCPERCIVA_DIR}/util

test:	test_warnp
	./test_warnp

.include <bsd.prog.mk>
